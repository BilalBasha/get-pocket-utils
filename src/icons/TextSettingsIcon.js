import React from 'react';
import Icon from '../helpers/Icon';
import Add from '../Icons/Add';

const AddIcon = (props) => {
    return (
        <Icon {...props}>
            <Add></Add>
        </Icon>
    )
}

export default AddIcon;