import React from 'react';
import Icon from '../helpers/Icon';
import TwitterColor from '../Icons/TwitterColor';

const TwitterColorIcon = (props) => {
    return (
        <Icon {...props}>
            <TwitterColor></TwitterColor>
        </Icon>
    )
}

export default TwitterColorIcon;