import React from 'react';
import Icon from '../helpers/Icon';
import IosShare from '../Icons/IosShare';

const WebViewIcon = (props) => {
    return (
        <Icon {...props}>
            <IosShare></IosShare>
        </Icon>
    )
}

export default WebViewIcon;