import React from 'react';
import Icon from '../helpers/Icon';
import IosShare from '../Icons/IosShare';

const LinkedinMonoIcon = (props) => {
    return (
        <Icon {...props}>
            <IosShare></IosShare>
        </Icon>
    )
}

export default LinkedinMonoIcon;