import React from 'react';
import Icon from '../helpers/Icon';
import Tag from '../Icons/Tag';

const TagIcon = (props) => {
    return (
        <Icon {...props}>
            <Tag></Tag>
        </Icon>
    )
}

export default TagIcon;